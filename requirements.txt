yfinance>=0.2.40
requests>=2.32
python-dotenv>=1.0
pandas>=2.2
psycopg2-binary>=2.9
SQLAlchemy>=2.0
pydantic>=2.7
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
# Pinned (not >=) to the exact versions verified against Python 3.14: an
# open-ended range previously let pip silently pull a newer dbt-core/
# dbt-adapters release with its own mashumaro ceiling, which could reintroduce
# the incompatibility below with no local repro.
dbt-core==1.11.12
dbt-postgres==1.10.2
# NOTE: dbt-core pins mashumaro<3.15, but that range crashes on Python 3.14
# (mashumaro.exceptions.UnserializableField on dbt_common's JSON schema
# build). mashumaro>=3.18 fixes it, but pip's resolver refuses to install
# `mashumaro>=3.18` in THIS file alongside dbt-core in one pass (dbt-core's
# own declared ceiling makes the combined request unsatisfiable). The
# override must be applied as a SEPARATE step after this file installs:
#   pip install -r requirements.txt
#   pip install --upgrade "mashumaro>=3.18,<4.0"
# (see README setup instructions and every CI workflow for this two-step
# install; do not add a mashumaro line to this file, it will break
# `pip install -r requirements.txt` for everyone).
ruff>=0.5
pytest>=8.2
pytest-mock>=3.14
