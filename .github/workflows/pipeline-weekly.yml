name: Pipeline semanal (FRED + dbt)

on:
  schedule:
    # Lunes 06:00 UTC.
    - cron: "0 6 * * 1"
  workflow_dispatch: {}  # permite ejecutarlo a mano

jobs:
  el-and-transform:
    name: EL Python (FRED) + dbt build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
          cache: "pip"

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      # dbt-core's own mashumaro<3.15 ceiling breaks on Python 3.14; the
      # override must run as a second pip step (see requirements.txt).
      - name: Aplicar override de mashumaro (Python 3.14)
        run: pip install --upgrade "mashumaro>=3.18,<4.0"

      - name: EL — extraer y cargar observaciones FRED a openfin_raw
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
        run: python -m src.el.pipeline fred

      - name: Derivar variables PG* desde DATABASE_URL
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: python scripts/parse_database_url.py >> "$GITHUB_ENV"

      - name: dbt deps
        working-directory: dbt
        run: dbt deps

      - name: dbt build (transforma openfin_raw -> openfin + tests)
        working-directory: dbt
        run: dbt build
